<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Beadle.Core.Views.MainPage"
             Title="Beadle">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Lates"/>
            <Label Text="{Binding ListTitle}"/>
            <ListView x:Name="StudentListView" ItemsSource="{Binding Classmates}" 
                      SelectedItem="{Binding SelectedStudent}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding FullName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Open new Page" Command="{Binding ShowAddPageCommand}" HorizontalOptions="Center" />
        </StackLayout>
        
    <!--<StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>-->
    </ContentPage.Content>
</ContentPage>